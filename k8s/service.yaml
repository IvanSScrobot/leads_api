apiVersion: v1
kind: Service
metadata:
  name: ardent-intake-api
  namespace: default
  labels:
    app: ardent-intake-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: ardent-intake-api
